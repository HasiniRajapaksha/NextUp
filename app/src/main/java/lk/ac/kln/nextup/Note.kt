package lk.ac.kln.nextup

// data class that represents a single note in the application
// it contains an ID, a title, and the content of the note
data class Note(
    val id: Int,
    val title: String,
    val content: String
)
