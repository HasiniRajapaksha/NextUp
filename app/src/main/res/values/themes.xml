<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NextUp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/purpleL</item>
        <item name="colorPrimaryVariant">@color/purpleL</item>
        <item name="android:statusBarColor">@color/purpleL</item>

    </style>

    <style name="Theme.NextUp" parent="Base.Theme.NextUp" />

</resources>